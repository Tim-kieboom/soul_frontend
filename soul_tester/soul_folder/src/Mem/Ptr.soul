extern "C" __clib_bytesOffset(self: *u8, index: int): *u8

RawOffset(self: *u8, offset: int): *u8 {
    __clib_bytesOffset(self, offset)
}

Offset<T>(self: *T, offset: int): *T {
    literal SIZE = int.sizeof as int

    ptr := __clib_bytesOffset(self as *u8, SIZE * offset) as *T
    ptr
}