import (
    crate.Test
    crate.Std.Io
)

use<T> [@]T impl IndefRef<T> {
    IndexRef(@this, index: uint): @T {
        Io.Assert(this.len <= index, msg: "index out of bounds")
        @*intrinsic.PtrOffset(this as *T, index)
    }
}

main() {
    Test.RunTests()
    Io.Println("hello world")
}
