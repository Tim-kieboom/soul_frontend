import ( 
    crate.Io
    // crate.Test
    // crate.Fs.{File, Open, FileMode}
    crate.Mem.Slice.{Index}
    crate.Mem.Ptr.{RawOffset}
)

main() {
    // Test.RunTests()

    // Struct.Methode()
    // File.Open(c"text.txt", FileMode.ReadOnly)

    array := [1, 2, 3, 4]
    ptr := PtrOffset(array as *int, 1)
    num := *ptr
    // mut index = Index<int>(@array, 0)
    Io.Println_int(num as int)

    // Io.Println("hello world")
}