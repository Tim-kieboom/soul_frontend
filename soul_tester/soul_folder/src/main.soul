import ( 
    crate.Io
    crate.Test
    crate.Mem.Slice.{Index}
)



main() {
    Test.RunTests()

    // Struct.Methode()
    // File.Open(c"text.txt", FileMode.ReadOnly)
    array := [1, 2, 3, 4]

    mutRef := IndexMut<int>(&array, 1)
    *mutRef = 20

    ref := Index<int>(@array, 1)
    mut num = *ref
    Io.Println_int(num as int)




    // Io.Println("hello world")
}