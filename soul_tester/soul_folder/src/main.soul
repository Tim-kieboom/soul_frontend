import ( 
    crate.Io
    crate.Test
    crate.Lib.SubLib
    crate.Fs.{File, READ_ONLY}
    crate.Lib.{this, Variable, A, B as b}
)

main() {
    b()
    Test.RunTests()

    file := File.Open(c"test.txt", READ_ONLY)
    file.Print()
    file.Close()

    infer := 1
    mut num = A()
    num = b()
    num = Lib.A()
    num = Lib.B()
    num = Variable
    num = Lib.Variable
    num = SubLib.Constant

    Io.Println("hello world")
}