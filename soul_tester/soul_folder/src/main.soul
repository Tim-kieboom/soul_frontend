import ( 
    crate.Io
    crate.Test
    crate.Lib.SubLib
    crate.Fs.{File, Open, ReadLen, Print, Close, READ_ONLY}
    crate.Lib.{this, Variable, A, B as b}
)

main() {
    b()
    Test.RunTests()

    file := File.Open(c"test.txt", READ_ONLY)
    len := file.ReadLen()
    Io.Println_int(len)
    file.Close()

    infer := 1
    mut num = A()
    num = b()
    num = Lib.A()
    num = Lib.B()
    num = Variable
    num = Lib.Variable
    num = SubLib.Constant

    Io.Println("hello world")
}