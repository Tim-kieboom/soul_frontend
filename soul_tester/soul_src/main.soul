import ( 
    crate.subLib.{this}
    crate.Lib.{this, Variable, A, B as b}
)

function() {}
// A() {}

main() {
    // function()
    // obj := Struct{field: 1}
    obj2 := Lib.Struct{field: 1}
    // mut num = A()
    // num = b()
    // num = Lib.A()
    // num = Lib.B()
    // num = Variable
    // Lib.Variable
    // Lib.priv()
    // subLib.Pub()
    // Lib.subLib.Pub()
    // fmt.Println("hello world")
}
